spring.application.name=Secure_Note

spring.security.user.name=user
spring.security.user.password=password

spring.datasource.url=jdbc:mysql://localhost:3306/securenotes
spring.datasource.username=root
spring.datasource.password=Rous123@.com

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

#for printing log detail in console.
logging.level.org.springframework.security=DEBUG

# JWT properties
spring.app.jwtSecret=${JWT_SECRET}
# 2 days
spring.app.jwtExpirationMs=172800000

# Frontend URL
frontend.url=http://localhost:3000

# Configuration for sending email
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=rk04393@gmail.com
spring.mail.password=${APP_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# GitHub OAuth2 configuration
spring.security.oauth2.client.registration.github.client-id=${GIT_ID}
spring.security.oauth2.client.registration.github.client-secret=${GIT_KEY}
spring.security.oauth2.client.registration.github.scope=read:user,user:email

# Google OAuth2 configuration
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}

# AI
gemini.api.key=${GEMINI_KEY}
gemini.api.url=${GEMINI_URL}

google.search.api.key=${GOOGLE_SEARCH_KEY}
google.search.cse.id=${GOOGLE_SEARCH_ID}